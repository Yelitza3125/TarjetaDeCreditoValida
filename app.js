function isValid(str) {
	while((typeof(str) === "number")) {
		var strInvertido = str.reverse();
	for ( var i = 2; (i % 2 === 0); i +2) {
		if(strInvertido[i] * 2) {
		var number = strInvertido.split(strInvertido[i] >= 10);
		 }		
		}
	}
	
	function sum();
	
}
